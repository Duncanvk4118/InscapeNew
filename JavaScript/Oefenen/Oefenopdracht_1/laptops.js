class Laptops {
  constructor(name, price, merk, type, storage, ram, driver, lid) {
    (this.name = name),
      (this.price = price),
      (this.type = type),
      (this.storage = storage),
      (this.ram = ram),
      (this.driver = driver),
      (this.lid = lid),
      (this.merk = merk);
  }
}

export default Laptops;
